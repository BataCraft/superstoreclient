/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com'], // Add the external domains you need here
      },
};

export default nextConfig;
